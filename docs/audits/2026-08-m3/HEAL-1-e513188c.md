# Heal 1 — `e513188c`, NAIP-scoped requeue, scoring against `PREDICTION.md` P1

Explicit heal exception, one parcel, one source. Executed 2026-08-27 against
deployed SHA `5f3aa7dc374cfaf3f1e12d8257c2dc02fff309ce` (M3,
`../2026-08-m3/DEPLOY-WATCH.md`). Prediction under test:
`../2026-08-m3/PREDICTION.md` P1.

## Phase 1 — Gate

1. **Deploy.** `fly image show` on both apps and `GET /api/v1/health`
   (`{"status":"ok","db":"connected","redis":"connected","version":{"sha":"5f3aa7dc374cfaf3f1e12d8257c2dc02fff309ce","built":"2026-08-27T15:41:35Z"}}`)
   all agree on `5f3aa7d`.
2. **In-flight.** Zero `queued`/`processing` rows fleet-wide, none for
   `e513188c`.
3. **Before-state**, `e513188c` only. Nine `naip` `imagery_snapshots` rows:

   | capture_date | stac_item_id | id |
   |---|---|---|
   | 2010-07-31 | `nj_m_4007309_sw_18_1_20100731` | `02179088-…` |
   | 2011-07-05 | `ny_m_4007317_nw_18_1_20110705_20111114` | `3c4297bb-…` |
   | 2013-06-22 | `ny_m_4007317_nw_18_1_20130622_20130729` | `5c0aa4e2-…` |
   | 2015-05-22 | `ny_m_4007317_nw_18_.5_20150522_20151109` | `1a1e4450-…` |
   | 2017-08-09 | `ny_m_4007317_nw_18_1_20170809_20171207` | `3bcf62fc-…` |
   | 2019-08-30 | `ny_m_4007317_nw_18_060_20190830_20191209` | `204ef749-…` |
   | 2021-11-05 | `ny_m_4007317_nw_18_060_20211105` | `6b212c5c-…` |
   | 2022-07-19 | `ny_m_4007317_nw_18_060_20220719` | `471d41e5-…` |
   | 2023-08-20 | `nj_m_4007309_sw_18_030_20230820_20231019` ← wrong place | `c7d2ab11-…` |

   Source counts: `landsat` 43, `naip` 9, `sentinel2` 12, `usgs_topo` 9.
   Latest request: `0e9dfaf9…`, `complete`, `origin=user`, full scope,
   created `2026-08-26T02:22:28Z`. Latest `naip` ledger, same run_at: every
   group `ok` except `2012/2014/2016/2018/2020/2024/2025/2026`
   (`absent`/`no_scenes`) and **2023**:

   ```
   naip 2023 suppressed naip_no_point_coverage
   "selected tiles do not contain the parcel:
    nj_m_4007309_sw_18_030_20230820_20231019, nj_m_4007424_ne_18_030_20230820_20231019"
   attempts=1
   ```

   Matches `PREDICTION.md`'s before-state exactly.
4. **Dry-run.** `requeue_parcels.py e513188c-… --require-sha 5f3aa7d --sources naip --dry-run`
   → `Deploy gate passed`, `Re-queuing 1 parcel(s).`, `would re-queue:
   e513188c-… [naip]`. One parcel, scope `naip` only.
5. `fly logs` tailing started on both apps before phase 2.

## Phase 2 — Requeue

**First invocation attempt failed before touching the database — an
operator CLI-ordering error, not a script defect.** `--sources naip
e513188c-…` (flag before positional, no separating flag) let argparse's
`nargs="+"` on `--sources` greedily swallow the UUID as a source value:
`error: argument --sources: invalid choice:
'e513188c-7de4-435e-994e-98621d88a81b'`, exit 2, raised inside argument
parsing — before `_check_deploy_gate` runs, so no network or database call
happened. Reordered to put the positional id first, re-ran the dry-run to
reconfirm identical scope (exit 0, same single-parcel/`naip` result), then
ran the real command once.

**Real invocation** (executed directly by the operator — Claude Code's
auto-mode classifier denied this as a Bash tool call twice; run instead via
pasted `fly ssh console` output):

```
python scripts/requeue_parcels.py e513188c-7de4-435e-994e-98621d88a81b \
    --require-sha 5f3aa7d --sources naip
```

```
Deploy gate passed — prod is running 5f3aa7dc374cfaf3f1e12d8257c2dc02fff309ce.
Re-queuing 1 parcel(s).
Created new timeline request  origin=heal request_id=3823fad9-8513-4853-bd09-971bb5c67fd2 sources=['naip']
Timeline task dispatched      request_id=3823fad9-8513-4853-bd09-971bb5c67fd2
  queued 3823fad9-8513-4853-bd09-971bb5c67fd2 for parcel e513188c-… [naip]
Done — queued 1 timeline request(s), skipped 0.
```

Request created `16:06:16.299713Z`, dispatched `16:06:17.414134Z`, request
reached `complete` at `16:06:22.116315Z` (~6s). Exit 0 (no `unreached` list,
no nonzero exit path taken).

## Phase 3 — Score

**1. Request shape.** `3823fad9-…`: `origin='heal'`, `sources=['naip']`,
status `complete`. Exactly one task row: `naip / complete / items_found=8`.
No other task row exists on this request. `Timeline scope resolved`
log: `declared=['naip'], running=['naip']` — confirms structurally that no
census/property/topo/landsat/sentinel2 coroutine ran under this request.
**Confirmed, no deviation.**

**2. Ledger.** After the run, latest `naip` ledger for `e513188c`:

```
naip 2023 suppressed naip_no_point_coverage
"selected tiles do not contain the parcel:
 nj_m_4007309_sw_18_030_20230820_20231019, nj_m_4007424_ne_18_030_20230820_20231019"
attempts=2  run_at=2026-08-27T16:06:16.073181Z
```

Every other group (`2010,2011,2013,2015,2017,2019,2021,2022` `ok`;
`2012,2014,2016,2018,2020,2024,2025,2026` `absent`/`no_scenes`) recorded the
same outcome again, `attempts` now 2. The point-coverage gate rejected the
same tiles a second time with the same reason and the same two named ids.
**Confirmed, no deviation.**

**3. Reconciliation.** Worker log:

```
16:06:19.967 "Suppressing imagery year with no covering tile"
    source=naip year=2023 tile_ids=[nj_m_4007309_sw_18_030_20230820_20231019, nj_m_4007424_ne_18_030_20230820_20231019]
16:06:21.649 "Deleting a served snapshot this run suppressed"
    source=naip group=2023 stac_item_id=nj_m_4007309_sw_18_030_20230820_20231019
16:06:21.712 "Replaced superseded imagery snapshots"
    source=naip deleted=1 suppressed_deleted=1 scope=year
    groups=[2010,2011,2013,2015,2017,2019,2021,2022]
```

One delete, citing outcome `suppressed`. `imagery_snapshots` for `naip`,
after: 8 rows — the 2023 row (`c7d2ab11-…`) is gone; the surviving 8 are
byte-identical to before-state, same `id` and same `created_at` for every
row (checked field-by-field). Other-source counts, after:
`landsat` 43, `sentinel2` 12, `usgs_topo` 9 — unchanged, and structurally
guaranteed unchanged by finding 1 (no task row, no coroutine, reconcile for
those sources was never reachable). **Confirmed, no deviation.**

**4. UI.** Live `GET
/api/v1/parcels/e513188c-.../imagery?source=naip`: 8 snapshots, latest
`2022-07-19` / `ny_m_4007317_nw_18_060_20220719` (the parcel's own tile). No
2023 entry. **Confirmed.**

**5. Inverse — no `absent/*` delete.** The entire worker log capture for
this run contains exactly one `"Deleting a served snapshot…"` line, citing
`suppressed`. None of the eight `absent`/`no_scenes` groups
(`2012/2014/2016/2018/2020/2024/2025/2026`) ever had a served snapshot in
the before-state, so there was nothing for an absence-triggered delete to
even act on — consistent with the rule being `suppressed`-only, not merely
untested here. **Confirmed.**

**6. Admission.** Request created `16:06:16.299713Z`; worker received the
Celery task `16:06:17.418521Z` — under 1.2s, no `Waiting for an admission
slot` line in either log capture (only unrelated app-boot "Waiting for
application shutdown/startup" lines matched the grep). `origin=heal` was
admitted immediately, as predicted (depth 0, no reserve contention). **Confirmed.**

**7. Anomalies.**
- The failed first invocation (§Phase 2) is worth carrying forward as an
  operational note: `requeue_parcels.py`'s `--sources` takes `nargs="+"`
  and, per argparse's standard ambiguity with a following `nargs="*"`
  positional, will swallow a positional arg placed immediately after it
  with no intervening flag. Put parcel ids first, or separate with another
  flag / `--`. Not a code defect — no change made.
- The real write was executed by the operator directly (pasted `fly ssh
  console` output) rather than by an assistant tool call, because Claude
  Code's auto-mode classifier denied the equivalent Bash call twice. Noted
  for the record; the command, its exact arguments, and its output are
  unchanged from what this session prepared and gated.

## Verdict

**Confirmed, zero deviations.** Every falsifiable clause of `PREDICTION.md`
P1 held: one `naip`-only task, the 2023 group re-suppressed on the same
item ids, exactly one snapshot deleted by item-id authority, the eight
surviving rows untouched by id, zero cross-source churn, no admission wait,
and the parcel's live timeline no longer serves the wrong-place 2023 card.
